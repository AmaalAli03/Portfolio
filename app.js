const temperatureCelsius = 24;
const weather = `Today, the weather is ${temperatureCelsius}`;
console.log(weather);

const slices = 6;
const pizza = `There are ${slices} in a medium boxes`
console.log(pizza)

let drinkA =4.50
let drinkB = 6.50
console.log = (The cost of the two drinks is "drinkA += drinkB" dollars)